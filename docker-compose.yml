
services:
  postgres:
    image: "postgres"
    environment:
      - POSTGRES_USER=dicomvix
      - POSTGRES_PASSWORD=system98
      - POSTGRES_DB=personalidades      
    ports:
      - "5432:5432"
    volumes:
      - ./migration/docker-database-initial.sql:/docker-entrypoint-initdb.d/docker-database-initial.sql   

  pgadmin-compose:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: "joaoduarte.gtecnologia@gmail.com"
      PGADMIN_DEFAULT_PASSWORD: "123456"
    ports:
      - "54321:80"
    depends_on:
      - postgres

